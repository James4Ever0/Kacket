Program := {Expr}+
Expr := Var | DefineExpr | IfExpr | ProcedureExpr | LetExpr  | CallExpr  | Const
DefineExpr := ( define ( Identifier {Identifier}* ) Expr )
            | ( define Identifier Expr )
IfExpr := ( if Expr Expr Expr )
ProcedureExpr := ( lambda ( {Identifier}* ) {Expr}+ )
LetExpr := ( let ( {Identifier Expr}+ ) {Expr}+ )
VarExpr := Identifier
Const := String | Number | Symbol